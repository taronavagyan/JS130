// main.js

const { addNumber } = require("./addNumber");
const { sumOfSquares } = require("./sumOfSquares");

addNumber(1);
addNumber(5);
addNumber(-3);
addNumber(4);
console.log(sumOfSquares()); // => 51
